---
title: "Getting Started"
description: "Spin up JSON Server from the external json-server-docs repo."
order: 1
---

# Getting Started

Create a `db.json` and start the server:

```bash
npx json-server --watch db.json
```

When this file is synced, the hub serves it at `/json-server/docs/getting-started/`.
