---
title: "JSON Server Docs"
description: "Sample JSON Server documentation contributed from an external repo."
order: 0
---

# JSON Server

This page is authored in the `examples/json-server-docs` repo and synced into the
hub's `src/content/docs/json-server/` by the `sync-docs.yml` workflow. The product
segment (`json-server`) is added at sync time from the workflow's `PRODUCT` env var.

See the [Getting Started](./getting-started.md) page for a quick tour.
